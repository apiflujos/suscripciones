{
  "name": "wompi-subs",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/admin",
    "dev:api": "npm run dev -w apps/api",
    "dev:admin": "npm run dev -w apps/admin",
    "build": "npm run build -w apps/api && npm run build -w apps/admin",
    "build:api": "npm run build -w apps/api",
    "build:admin": "npm run build -w apps/admin",
    "start": "npm run start -w apps/api & npm run start -w apps/admin",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/admin",
    "typecheck": "npm run typecheck -w apps/api && npm run typecheck -w apps/admin",
    "db:generate": "npm run db:generate -w apps/api",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:push": "npm run db:push -w apps/api",
    "db:studio": "npm run db:studio -w apps/api",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

-- Migration SQL generated by Prisma
-- Migration name: add_actor_to_logs_and_messages
-- Date: 2026-03-10

-- Add actor column to SystemLog table for audit trail
ALTER TABLE "SystemLog" ADD COLUMN IF NOT EXISTS "actor" TEXT;

-- Add actor column to ChatwootMessage table for notification traceability
ALTER TABLE "ChatwootMessage" ADD COLUMN IF NOT EXISTS "actor" TEXT;

-- Create indexes for efficient querying by actor
CREATE INDEX IF NOT EXISTS "SystemLog_actor_createdAt_idx" ON "SystemLog"("actor", "createdAt");
CREATE INDEX IF NOT EXISTS "ChatwootMessage_actor_createdAt_idx" ON "ChatwootMessage"("actor", "createdAt");
